package com.trendpulse.text_enricher;

public class LanguageDetectorStub {
    static String detectLanguage(String text) {
        // This is a stub method for language detection.
        // In a real implementation, you would use a library or service to detect the language.
        // For now, we will return "en" (English) as a placeholder.
        return "en";
    }   
}
