spring.application.name=realtime-push

server.port=8086

# Kafka configuration
spring.kafka.bootstrap-servers=localhost:9092
spring.kafka.properties.schema.registry.url=http://localhost:8081

# Consumer group for realtime-push
spring.kafka.consumer.group-id=realtime-push